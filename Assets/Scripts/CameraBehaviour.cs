using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraBehaviour : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
	/*var gyro = Input.gyro;
	if(!gyro.enabled)
	{
		gyro.enabled = true;
	}
		var quaternion = gyro.attitude;
		gameObject.transform.rotation = gyro.attitude * Quaternion.AngleAxis(90, new Vector3(0,0,1));
		Debug.Log("Rotation camera : " + gameObject.transform.rotation);*/
	}
}
